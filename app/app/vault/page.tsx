import { PagePlaceholder } from "@/components/shell";

export default function VaultPage() {
  return (
    <PagePlaceholder
      description="Vault positions, lock preparation, unlock eligibility, and wallet-signature handoff are scheduled for phase 14."
      eyebrow="Vault"
      title="Locked positions"
    />
  );
}
