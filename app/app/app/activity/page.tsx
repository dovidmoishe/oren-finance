import { PagePlaceholder } from "@/components/shell";

export default function ActivityPage() {
  return (
    <PagePlaceholder
      description="A dedicated activity route will expand the portfolio feed into a full wallet and Oren execution timeline."
      eyebrow="Activity"
      title="Activity timeline"
    />
  );
}
